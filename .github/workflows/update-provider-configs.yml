name: Test Claude API
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - run: echo "test"  
    - name: Test curl
      env:
        ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      run: |
        curl --version
        echo "Testing simple curl"
